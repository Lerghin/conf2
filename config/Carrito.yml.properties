server:
  port: 9097
spring:
  application:
    name: carrito
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
  datasource:
    url: jdbc:mysql://localhost:3306/cart_service?useSSL=false&serverTimezone=UTC
    username: root
    password: ""  # Assuming password is empty
eureka:
  client:
    service-url:
      defaultZone: http://eureka-sv:8761/eureka